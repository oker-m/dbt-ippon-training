version: 2

sources:
  - name: name_source_scenario2
    database: DB_DBT_MOKER_DEV
    schema: source

    tables:
      - name: restaurants
        columns:
        - name: open_on_sunday
          tests:
            - not_null
        - name: address
          tests:
            - not_null  
      - name: orders
        columns:
        - name: identifier
          tests:
          - unique
        - name: restaurant_identifier
          tests:
          - relationships:
              to: ref('base_restaurants')
              field: identifier
      - name: dishes
          