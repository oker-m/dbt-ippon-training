version: 2

models:
  - name: restaurants__turnover_ordered
    description: toto
    tests:
      - dbt_utils.equality:
          compare_model: ref('expects_restaurants__turnover_ordered')
          tags: ['unit_testing']